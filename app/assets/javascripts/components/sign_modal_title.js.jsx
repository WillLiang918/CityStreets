( function (root) {
  var SignModalTitle = root.SignModalTitle = React.createClass({
    render: function () {
      return (
        <div className="sign-in-up-modal-navbar">
          TITLE
        </div>
      );
    }
  });
})(this);
